
/*
function makeSandwichWith (){
    let beleg = 'ham';
    let brood = 'bruin';

    console.log(beleg + brood);
}

makeSandwichWith(); */

/*
const myFunction = makeSandwichWith2 (){
    let beleg = 'kaas';
    let brood = 'wit';

    console.log("there you go, a sandwich with " + beleg  + '' + brood);
}

makeSandwichWith2(); 
 */
/*
 const calculateDiscountedPrice = function() {
     console.log(a);
    a();
 };

 calculateDiscountedPrice (function() {
    let b = 10;
    let c = 1;
    console.log (b - c);
 });
*/

//deel C

const calculateDiscount = function (totalAmount, discount) {
    console.log(calculateDiscount);
    return totalAmount - discount;
}
    calculateDiscount(50, 10);


//deel D
 const calculateDiscount = function (totalAmount, discount) {
    if (totalAmount > 25) {
        console.log(calculateDiscount);
        return totalAmount - discount;
    } else {
        console.log(calculateDiscount.totalAmount);
        return totalAmount - discount;
    }
}
calculateDiscount(50, 10);

